#!/bin/sh

BACKGROUND="y"

if [ "$BACKGROUND" = "y" ]; then
    java -Xmx1024M -cp $DYNINST_ROOT/craft/viewer ConfigEditorApp $@ &
else
    java -Xmx1024M -cp $DYNINST_ROOT/craft/viewer ConfigEditorApp $@
fi

