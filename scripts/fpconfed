#!/bin/sh

BACKGROUND="y"

if [ "$BACKGROUND" = "y" ]; then
    java -Xmx1024M -jar $DYNINST_ROOT/craft/viewer/fpconfed.jar $@ &
else
    java -Xmx1024M -jar $DYNINST_ROOT/craft/viewer/fpconfed.jar $@
fi

